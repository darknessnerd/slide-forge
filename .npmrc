# .npmrc — GitHub Packages registry for @team scoped packages
# GITHUB_TOKEN must be set in the environment (CI: use secrets, local: personal access token)
# Scope this to @team only — public packages still resolve from registry.npmjs.org
@team:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
